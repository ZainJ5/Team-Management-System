// const pool = require('../config/db')

// exports.createteam = async ()=>{
//     const { user_id } = req.body.user_id
//     const res = pool.query('Insert into teams values ($1)',user_id)
//     return res.status(200).send(res.team_id)
// }
